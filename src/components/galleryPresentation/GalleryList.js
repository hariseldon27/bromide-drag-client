import React from 'react'
import GalleryListCard from './GalleryListCard'


function GalleryList() {
    //takes data and passes it through map, and spits out
    //card for each piece t mapped over
  return (
    <div>GalleryList</div>
  )
}

export default GalleryList